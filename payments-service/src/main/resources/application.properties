spring.application.name=payments-service

# Server Configuration
server.port=8083

# Datasource Configuration (if payments service has its own DB, otherwise remove/comment out)
# spring.datasource.url=jdbc:mysql://localhost:3306/your_payment_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# spring.datasource.username=your_mysql_username
# spring.datasource.password=your_mysql_password
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Payment Gateway Configuration (Example placeholders - UPDATE WITH ACTUAL VALUES)

# Security Configuration for Resource Server
jwt.shared.secret=yourSameJwtSecretKeyAsInUsersServiceWhichIsVeryLongAndSecureAndBase64EncodedIfNecessary # This is a custom property

# Payment Gateway Configuration (Example placeholders - UPDATE WITH ACTUAL VALUES)
payment.gateway.apiKey=YOUR_PAYMENT_GATEWAY_API_KEY
payment.gateway.secretKey=YOUR_PAYMENT_GATEWAY_SECRET_KEY
payment.gateway.endpointUrl=https://api.paymentgateway.com/v1/
